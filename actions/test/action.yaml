name: test
author: yhpark01
description: 'Use test module in github action'
inputs:
  VERSION:
    description: test version
    required: true

runs:
  using: "composite"
  steps:
  - name: shell script function test
    shell: bash
    run: |
      test() {
        echo "hello"
        echo ${{ inputs.VERSION }}
      }
    
      test
