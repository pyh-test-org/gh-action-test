name: test
author: yhpark01
description: 'Use test module in github action'

runs:
  using: "composite"
  steps:
  - name: shell script function test
    shell: bash
    run: |
      test() {
        echo "hello"
      }
    
      test
